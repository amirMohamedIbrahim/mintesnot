using System;

namespace JeapordyWorld
{
    class Program
    {

string str;
int abc;
string largest;
string currentWord;
string[] WordsInLine;
string line;


Console.Writeline("Enter the Sentence, So we can find the longest Word!");
string str = Console.ReadLine();
Type abc = Type.Parse(str);

currentWord = WordsInLine[r];
if( currentWord.length() > largest.length())
{
  largest = currentWord;
}
    }
    }
